/* const express = require("express");
const supertest = require("supertest");

function testServer(route) {
    const app = express();
    route(app);
    return supertest(app);  // Devuelve la instancia de supertest para hacer peticiones
} */

/* module.exports = testServer;
const supertest = require("supertest");
const app = require('../app');  // Importa la app exportada

module.exports = supertest(app);  */

